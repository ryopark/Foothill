<%= form_for @sell_book_comment, url: sell_book_comments_path(sell_book_id: params[:sell_book_id]) do |f| %>
  <%= f.text_field :body, placeholder: 'Enter comment' %>
  <%= f.submit 'Post', class: 'btn btn-default' %>
<% end %>
